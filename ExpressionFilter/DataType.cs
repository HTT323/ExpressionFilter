namespace ExpressionFilter
{
    public enum DataType
    {
        Expression,
        Integer,
        String,
        Decimal,
        Double,
        Boolean,
        DateTime,
        Guid,
        Enum
    }
}